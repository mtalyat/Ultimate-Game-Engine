- Add the /// for all 'complete' functions and classes
	Completed: Point

- Copy Size to reflect all operators of Point

- Queue WaitForKey Inputs... maybe

- Fix platform collisions, reset ignoring collisions when it doesn't hit the 
specific object and not just any object

- Fix collisions if Player is not Instantiated first

- Fix UI (MessageBox, specifically) not showing after setting Player's Z